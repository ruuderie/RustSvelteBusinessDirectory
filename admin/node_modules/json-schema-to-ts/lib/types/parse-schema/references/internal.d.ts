import type { ParseSchemaOptions } from "json-schema-to-ts/lib/types/parse-schema";
import type { ReferencingSchema } from "json-schema-to-ts/lib/types/parse-schema/references";
import type { ParseReference } from "json-schema-to-ts/lib/types/parse-schema/references/utils";
export type ParseInternalReferenceSchema<REFERENCING_SCHEMA extends ReferencingSchema, OPTIONS extends ParseSchemaOptions, PATH extends string> = ParseReference<Omit<REFERENCING_SCHEMA, "$ref">, OPTIONS, OPTIONS["rootSchema"], PATH>;
